# Content Blocker Suite — מסמך מצב מלא

**מטרת המסמך:** לאפשר לכל אדם או מודל AI, ללא רקע קודם על הפרויקט, להבין תוך דקות מה נבנה, למה, איך זה עובד, מה כבר נבדק ועבד, מה עוד פתוח, ואיפה כל דבר נמצא.

**עדכון אחרון:** 19.08.2026, גרסת agent נוכחית: **v1.8.0**

**Repository:** https://github.com/moshehadasa0040-lang/new1 (branch `main`)

---

## 1. מה המערכת עושה (בקצרה)

תוכנה לחסימת קבצי וידאו במחשבי Windows, המיועדת ל"מפקח" (הורה/מנהל) שמנהל **הרבה מחשבים** במקביל דרך דשבורד web מרכזי אחד. על כל מחשב מותקן "agent" (תוכנת רקע) שחוסם באופן **אמיתי וקשה לעקיפה** גישה לקבצי וידאו מקומיים. המפקח יכול, מרחוק, לפתוח זמנית מחשב ספציפי (למשל 15 דקות) כדי לאפשר צפייה מבוקשת, ולסגור אותו שוב אוטומטית או ידנית.

### דרישות מקוריות (כפי שהוגדרו על ידי המשתמש)
- חסימה **מוחלטת** של קבצי וידאו במחשב — לא רק "לחסום נגנים ידועים" אלא לוודא שאין שום דרך לפתוח קובץ וידאו, יהיה הנגן אשר יהיה.
- דשבורד web שמציג את כל המחשבים המנוהלים, עם זיהוי ייחודי לכל מחשב.
- אפשרות לפתוח זמנית מחשב ספציפי מרחוק.
- אפשרות להסיר את התוכנה מרחוק דרך הדשבורד.
- הדשבורד מזהה אוטומטית סטטוס "מחובר/מנותק" לכל מחשב, כולל זיהוי הסרה/פורמט.
- (עתידי, **לא נבנה עדיין בכוונה**): מנגנון שמונע הסרה רגילה של התוכנה ללא קוד שמונפק מהדשבורד — הוחלט לדחות את הבנייה שלו עד שהחסימה הבסיסית תוכח כעובדת מלא. **זה השלב הבא המומלץ.**

---

## 2. ארכיטקטורה

שני חלקים נפרדים, ב-repo אחד:

```
content-blocker-suite/
├── agent/          → תוכנה שמותקנת על כל מחשב Windows מנוהל
└── server/         → API + דשבורד web, פרוס ב-Render
```

### 2.1 השרת (`server/`)

- **Node.js + Express**, פרוס ב-**Render** (https://new1-q4bb.onrender.com) כ-Web Service רגיל (**לא** Blueprint).
- **מסד נתונים: Redis/Valkey חיצוני דרך Aiven** (לא Postgres של Render עצמו — הוחלט על כך במפורש באמצע הפרויקט). כל הגישה למסד דרך `server/src/store.js`.
- דשבורד סטטי (HTML/CSS/JS פשוטים, ללא framework) ב-`server/src/public/`.
- Session-based auth לדשבורד עם סיסמת מנהל יחידה (`ADMIN_PASSWORD`).
- ה-agent מתקשר עם השרת דרך endpoints נפרדים תחת `/api/agent/*`, מאומתים עם `X-Device-Id` / `X-Device-Token` headers (לא session — אלה טוקנים שה-agent מקבל בהרשמה הראשונה ושומר מקומית).

**משתני סביבה נדרשים ב-Render:**
| משתנה | מקור | הערה |
|---|---|---|
| `REDIS_URL` | Aiven, "Service URI" מלא (`rediss://default:PASS@host:port`) | **הסיסמה המקורית נחשפה בצ'אט בעבר ותוחלפה** |
| `ADMIN_PASSWORD` | נבחר ידנית | סיסמת כניסה לדשבורד |
| `SESSION_SECRET` | נבחר ידנית / `openssl rand -hex 32` | חתימת cookie |

**הגדרות Render:** Root Directory = `server`, Build Command = `npm install`, Start Command = `npm start`.

### 2.2 ה-agent (`agent/`)

- **Node.js**, נארז לקובץ `.exe` יחיד עם `pkg` (`agent/package.json` → `"bin": "src/index.js"`, סקריפט `npm run build`).
- מותקן דרך installer שנבנה עם **Inno Setup** (`agent/installer/setup.iss`).
- רץ כ**שירות Windows** דרך **NSSM** (Non-Sucking Service Manager) — **לא** node-windows (הוחלף, ראה סעיף 4 — היה באג יסודי).
- ה-installer מוריד את NSSM אוטומטית מ-`nssm.cc` בזמן ה-build ב-GitHub Actions (לא נשמר ב-repo).

**קבצי המקור העיקריים (`agent/src/`):**
| קובץ | תפקיד |
|---|---|
| `index.js` | הלולאה הראשית: heartbeat, טיפול בפקודות, main() |
| `config.js` | קבועים: כתובת שרת, רשימת תהליכים חסומים, סיומות וידאו, אינטרוולים |
| `identity.js` | ניהול device ID (מבוסס Machine GUID) וטוקן, וקריאת שם מותאם מההתקנה |
| `blocker.js` | חסימת תהליכים לפי שם (`taskkill`) — שכבת הגנה **משנית** |
| `fileLock.js` | **שכבת ההגנה העיקרית**: נעילת קבצי וידאו ברמת NTFS (`icacls`) |
| `api.js` | קליינט HTTP לתקשורת עם השרת |
| `logger.js` | לוג מקומי לקובץ + שליחה לדשבורד לפי דרישה |
| `uninstall.js` | הסרה עצמית (משמש בזרימת ה-uninstall מהדשבורד) |

**קבצי ה-installer (`agent/installer/`):**
- `setup.iss` — סקריפט Inno Setup. כולל: מסך שמבקש שם מותאם למחשב, התקנת שירות NSSM (אידמפוטנטית — מסירה שירות ישן לפני התקנה מחדש), טיפול בהסרה (`--unlock-files` + עצירת/הסרת שירות).
- `uninstall-helper.bat` — סקריפט batch שרץ *detached* (מנותק) כדי לעצור/להסיר את השירות אחרי שה-agent מסיים לפעול (בשימוש בזרימת ה-uninstall **מהדשבורד**, לא בהסרה הרגילה של Windows).

**Build אוטומטי:** `.github/workflows/build-installer.yml` — רץ ב-`windows-latest`, מוריד NSSM, אורז עם `pkg`, מקמפל עם Inno Setup, מעלה כ-artifact (ותג release אם דוחפים `git tag vX.Y.Z`).

---

## 3. איך החסימה עובדת בפועל (חשוב להבין)

**שתי שכבות הגנה, לא אחת:**

1. **חסימת קבצים ברמת NTFS (`fileLock.js`) — זו ההגנה האמיתית.**
   סורק את כל הכוננים (קבועים + נשלפים, לא רשת) כל 3 דקות, ומכל קובץ עם סיומת וידאו (`.mp4`, `.mkv`, `.avi`, `.mov`, `.wmv`, `.flv`, `.m4v`, `.mpg`, `.mpeg`, `.webm`) שולל הרשאת קריאה (`icacls /deny`) מקבוצות `BUILTIN\Users` ו-`Authenticated Users` (SIDs: `*S-1-5-32-545`, `*S-1-5-11`). **אף תוכנה, יהיה שמה אשר יהיה, לא יכולה לפתוח את הקובץ** — Windows עצמו מסרב, ברמת המערכת, לא ברמת האפליקציה.

2. **חסימת תהליכים לפי שם (`blocker.js`) — הגנה משנית/משלימה.**
   כל 3 שניות בודק אם רץ תהליך מרשימת חסומים (VLC, Windows Media Player, אפליקציית הווידאו המובנית של Windows וכו') והורג אותו. לא הגנה מספיקה לבד (ניתן לעקוף בשינוי שם קובץ), אבל תופסת מקרים שהחסימה ברמת קובץ לא מכסה (למשל תזמון).

### מגבלות אמיתיות, ידועות ומתועדות (לא באגים — מגבלות מובנות)
- **לא חוסם סטרימינג מדפדפן** (Netflix, YouTube) — אין קובץ מקומי לנעול. אפשר להוסיף חסימת דפדפנים שלמים דרך "ערוך רשימת חסימה" בדשבורד, אך זה חוסם את כל הגלישה, לא רק וידאו.
- **לא חוסם משתמש עם הרשאות Administrator** על אותו מחשב — יכול להחזיר לעצמו הרשאות (Take Ownership). מגבלה מובנית של Windows, לא של הקוד.
- **כוננים ברשת לא נסרקים** (בכוונה — כדי לא לפגוע במשאבים משותפים).

---

## 4. יומן שינויים מלא — כל באג שנמצא ותוקן (v1.0 → v1.8.0)

זהו החלק הכי חשוב להבנת "למה הקוד נראה ככה". כל שורה כאן מייצגת שעות של איתור באגים אמיתיים מתוך **לוגים אמיתיים** שהמשתמש שלח, לא ניחושים.

| גרסה | מה תוקן | הסבר טכני |
|---|---|---|
| — | ארכיטקטורה ראשונית | Postgres של Render → הוחלף ל-**Redis/Valkey של Aiven** לפי בקשת המשתמש |
| — | `node-windows` **לא עבד** | לא הצליח לרשום שירות Windows מתוך קובץ `.exe` ארוז (`pkg`), כי `pkg` יוצר מערכת קבצים וירטואלית ו-`node-windows` לא מוצא את node.exe/הסקריפט האמיתיים. **הוחלף כליל ב-NSSM.** |
| v1.2.0 | 401 חוזר בלי סוף | אם המכשיר נמחק מהדשבורד (או שה-Redis התאפס), הטוקן המקומי כבר לא תקף. הוספה זיהוי 401 → ניקוי זהות מקומית → הרשמה מחדש אוטומטית. |
| v1.2.0 | שירות לא עולה אחרי התקנה | `nssm install` נכשל בשקט אם כבר קיים שירות באותו שם (משאריות התקנות קודמות). Inno Setup לא מציג שגיאה כזו כברירת מחדל. **תוקן:** התקנה אידמפוטנטית (מסירה שירות ישן קודם). |
| v1.3.0 | `uninstall` לא עשה כלום | סקריפט ה-batch להסרה השתמש בפקודת `timeout` שדורשת קונסולה אמיתית — נכשלת מיידית כשמופעלת *detached*. **הוחלף ב-`ping` (טריק המתנה סטנדרטי).** |
| v1.3.0 | הסרה רגילה (Windows Settings) לא שחררה קבצים | רק ה-uninstall **מהדשבורד** שחרר קבצים; ההסרה הרגילה של Windows לא עברה בקוד הזה בכלל. **תוקן:** דגל `--unlock-files` שה-installer מפעיל על ה-exe *לפני* מחיקת קבצים. |
| v1.3.0→1.4.0 | Race condition: unlock "נבלע" ע"י סריקה שכבר רצה | סריקת דיסק מלאה לוקחת דקה-שתיים; הבדיקה "האם אני אמור לחסום" נעשתה פעם אחת בהתחלה בלבד. אם ביקשו unlock **באמצע** סריקה, הסריקה סיימה לנעול הכל בלי לדעת שהתבקש unlock. **תוקן:** בדיקה לפני **כל קובץ בנפרד** בתוך הסריקה, לא רק בהתחלה. |
| v1.4.0 | הסרה = מחיקה מלאה, לא תלוית "זיכרון" | הסרה הסתמכה על `locked-files.json` (מעקב מקומי) שיכול להיות לא שלם (למשל אם השירות נעצר באמצע סריקה לפני שנשמר לדיסק). **תוקן:** uninstall מריץ סריקה מלאה מחדש ומשחרר כל קובץ שנמצא, ללא תלות במעקב. |
| v1.5.0 | שם מכשיר בעברית יצא ג'יבריש | שימוש שגוי ב-`SaveStringToFile` של Inno Setup עם פרמטר Boolean רביעי שחשבתי שמשמעו "Unicode" — בפועל **אין** כזה פרמטר בפונקציה הזו, וה-Boolean התפרש כ"Append", מה שגרם לכתיבה ב-ANSI (קידוד שגוי לעברית). **תוקן:** מעבר לפונקציה הנכונה `SaveStringsToUTF8File`. |
| v1.6.0 | דשבורד: אין אינדיקציה ויזואלית ברורה, הסרה לא מוחקת מהדשבורד | נוספה נקודת סטטוס צבעונית (ירוק זוהר/אדום זוהר). נוסף endpoint חדש `/api/agent/unregister` שה-agent קורא לו בכל נתיב הסרה (גם רגילה וגם מהדשבורד) — מוחק את המכשיר מהדשבורד לגמרי, לא רק מסמן "מנותק". |
| v1.7.0 | "Access Denied" גם אחרי unlock מוצלח | אחרי **שעות** של בדיקות (עשרות מחזורי נעילה על אותם קבצים כל 3 דק'), הצטברו כמה שכבות הרשאת Deny על אותם קבצים — כי `lockFile()` הריץ `icacls /deny` מחדש גם על קבצים כבר נעולים, וזה **מוסיף** רשומה נוספת, לא מעדכן קיימת. `/remove:d` לא תמיד ניקה את כל השכבות. **תוקן:** (1) `lockFile()` מדלג על קבצים שכבר נעולים; (2) שחרור עובר ל-`icacls /reset` (איפוס מלא, לא הסרה סלקטיבית). |
| v1.8.0 | סריקת uninstall יכלה להחזיר 0 קבצים | `Get-ChildItem -Recurse` על כל כונן C: נתקל בתיקיות מוגנות (`System Volume Information` וכו') וקרס **לגמרי**, גם עם `-ErrorAction SilentlyContinue` על הפקודה עצמה. תועד בלוג אמיתי: `"Access is denied"` ואז `"Uninstall sweep: unlocked 0 video file(s)"`. **תוקן:** `$ErrorActionPreference` ברמת session שלם + `exit 0` מפורש; וגם — הסרה עכשיו משחררת גם מהמעקב הידוע **וגם** מסריקה מלאה (ביטחון כפול). |

**סטטוס בדיקה נכון לרגע כתיבת המסמך:**
- ✅ **פתיחה זמנית מהדשבורד — אושרה עובדת בפועל** (המשתמש בדק ופתח סרטון בהצלחה, לא רק בלוג).
- 🔲 **הסרה (Uninstall) עם v1.8.0 — נשלחה לבדיקה, טרם אושרה סופית על ידי המשתמש.**

---

## 5. פונקציונליות דשבורד (מה שכבר קיים)

לכל מכשיר ברשימה:
- **שם** (מותאם אישית, ניתן להזין בזמן ההתקנה או לשנות בדשבורד)
- **גרסת agent** מוצגת
- **סטטוס** מחובר/מנותק עם נקודה צבעונית
- **פתח ל-X דקות** — פתיחה זמנית (prompt לכמות דקות)
- **נעל מיד** — ביטול פתיחה זמנית לפני הזמן
- **ערוך רשימת חסימה** — שינוי רשימת תהליכים חסומים per-device
- **בקש לוגים** / **הורד לוגים** — שליפת לוג ה-agent המקומי לצורכי דיבאג מרחוק
- **הסר תוכנה** — uninstall מרחוק (מוחק גם מהדשבורד אוטומטית)
- **מחק מהדשבורד** — מחיקת הרשומה בלבד (לא נוגע בהתקנה בפועל)

---

## 6. מה עוד לא נבנה (במפורש, לפי בקשת המשתמש)

**מנגנון הגנה מפני הסרה (anti-removal):** המשתמש ביקש בשלב מוקדם מנגנון שבו הסרת התוכנה תדרוש **קוד** שמונפק מהדשבורד — כדי שילד/משתמש לא יוכל פשוט להסיר את התוכנה בעצמו. **הוחלט במפורש לדחות את הבנייה הזו** עד שהחסימה הבסיסית תוכח כעובדת אמין (ראה ציטוט המשתמש: *"את האפשרות הזו נבנה אחרי שנראה שהתוכנה עושה את העבודה"*). זה השלב הבא הטבעי ברגע שהמשתמש מאשר שההסרה/פתיחה עובדות יציב.

חשוב לזכור בבניית זה בעתיד: **אין דרך למנוע לחלוטין הסרה על ידי מי שיש לו הרשאות Administrator** על אותו מחשב (למשל דרך Safe Mode, מחיקת קבצים ידנית, אתחול מ-USB חיצוני) — כל מנגנון "קוד הסרה" יקשה משמעותית על הסרה מקרית/לא מיומנת, אך לא יבטיח הגנה מוחלטת נגד משתמש טכני עם הרשאות מנהל.

---

## 7. פרטי אבטחה / הערות תפעוליות חשובות

- **GitHub Personal Access Tokens** שימשו כמה פעמים בשיחה זו לדחיפת קוד ישירות (לא דרך Git רגיל של המשתמש) — **כל token כזה נחשף בצ'אט ויש לוודא שבוטל** (GitHub → Settings → Developer settings → Personal access tokens).
- **סיסמת Aiven Redis** נחשפה בצילום מסך בשלב מוקדם בפרויקט — **יש לוודא שהוחלפה** (Aiven console → Reset password) ושה-`REDIS_URL` ב-Render מעודכן בהתאם.
- קידוד עברית ב-Inno Setup דורש `SaveStringsToUTF8File` — **לא** `SaveStringToFile` (אין לו אופציית Unicode אמיתית).
- כל שינוי ב-`.github/workflows/*.yml` דרך API/token דורש הרשאת `workflow` מפורשת ב-token (לא כלול כברירת מחדל ב-fine-grained tokens).

---

## 8. איך להריץ/לבדוק הכל מקומית (לפני push)

```bash
# שרת
cd server
npm install
REDIS_URL=redis://localhost:6379 ADMIN_PASSWORD=test123 SESSION_SECRET=test PORT=3000 npm start

# agent - בדיקת build (על מכונת פיתוח, ה-.exe עצמו רץ רק ב-Windows בפועל)
cd agent
npm install
npm run build   # יוצר dist/content-blocker-agent.exe
```

לבדיקת endpoints השרת אפשר `curl` ישירות מול `/api/agent/register`, `/api/agent/heartbeat` וכו' — כל הראוטים מתועדים בקוד עם הערות (`server/src/routes/agent.js`, `server/src/routes/admin.js`).

---

## 9. סיכום מנהלים — למי שרוצה רק את התמונה הגדולה

בנינו מערכת שני-חלקים (agent + דשבורד) לחסימת וידאו אמיתית ברמת מערכת ההפעלה, לא רק חסימת תוכנות ידועות. עברנו **מסע ארוך של איתור באגים אמיתיים** מתוך לוגים אמיתיים (לא תיאורטי) — כמעט כל תיקון נבע ישירות מציטוט לוג קונקרטי שהמשתמש שלח, לא מניחוש. הפתיחה/חסימה **אושרו כעובדות בפועל**. ההסרה עם התיקון האחרון (v1.8.0) ממתינה לאישור סופי. השלב הבא המתוכנן: מנגנון קוד הגנה מפני הסרה, אחרי אישור שהבסיס יציב.
